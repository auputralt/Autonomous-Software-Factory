[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "forge-sandbox"
version = "0.1.0"
description = "Rust sandbox engine for Forge"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
bindings = "pyo3"
